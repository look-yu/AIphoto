# 摄影智评小程序 - 开发文档

## 1. 技术架构

### 1.1 整体方案：微信云开发

```
┌─────────────────────────────────────────────┐
│                  微信小程序                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │  首页   │ │  学院   │ │  评阅   │       │
│  └────┬────┘ └────┬────┘ └────┬────┘       │
│       │           │           │             │
│       └───────────┼───────────┘             │
│                   │                         │
│            ┌──────┴──────┐                  │
│            │  云数据库   │                  │
│            └─────────────┘                  │
└────────────────────┬───────────────────────┘
                     │
              ┌──────┴──────┐
              │   云函数    │
              └──────┬──────┘
                     │
         ┌───────────┼───────────┐
         │           │           │
   ┌─────┴─────┐ ┌───┴───┐ ┌────┴────┐
   │ 腾讯云AI  │ │ LLM   │ │ 云存储  │
   │ 图像分析 │ │ 对话  │ │ 图片   │
   └──────────┘ └───────┘ └────────┘
```

### 1.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | 原生小程序 | WXML + WXSS + JavaScript |
| 后端 | 云函数 | Node.js / Python |
| 数据库 | 云开发数据库 | JSON文档型 |
| 存储 | 云存储 | 用户图片 |
| AI能力 | 腾讯云AI + 第三方LLM | 图像分析 + 对话 |

---

## 2. AI能力接入

### 2.1 图像分析（腾讯云AI）

**服务选择**：腾讯云图像分析

| 功能 | API | 免费额度 |
|------|-----|---------|
| 图像构图评分 | 图像标签 | 每月1000次 |
| 图像质量评估 | 图像质量评分 | 每月1000次 |
| 物体识别 | 物体检测 | 每月1000次 |

**接入方式**：
- 云函数中调用腾讯云SDK
- 图片先上传至云存储，传递URL给AI服务

### 2.2 对话助手（LLM）

**服务选择**：第三方LLM API（如MiniMax、OpenAI等）

**接入方式**：
- 云函数封装API调用
- 注入摄影领域Prompt模板
- 支持多轮对话上下文

---

## 3. 数据库设计

### 3.1 集合设计

```
users              # 用户表
├── openid         # 用户唯一标识
├── avatar         # 头像
├── nickname       # 昵称
├── level          # 摄影水平
├── createdAt      # 注册时间
└── settings       # 设置（悬浮助手开关等）

works              # 作品表
├── userId         # 用户ID
├── imageUrl       # 图片地址
├── title         # 作品标题
├── reviewResult  # 评阅结果
├── score         # 综合评分
├── createdAt     # 上传时间
└── tags          # 标签

reviews            # 评阅记录表
├── workId         # 作品ID
├── dimensions     # 各维度评分
├── suggestions    # 改进建议
├── createdAt     # 评阅时间
└── modelVersion   # AI模型版本

courses            # 课程表
├── title          # 课程标题
├── level          # 难度等级
├── content        # 课程内容
├── duration       # 学习时长
└── order          # 排序

chat_history       # 对话历史
├── userId         # 用户ID
├── messages       # 对话记录
├── createdAt      # 创建时间
└── updatedAt      # 更新时间
```

### 3.2 权限设置

| 集合 | 权限 | 说明 |
|------|------|------|
| users | 仅创建者可读写 | 隐私数据 |
| works | 创建者可写，所有人可读 | 作品公开 |
| reviews | 跟随作品 | 随作品权限 |
| courses | 所有人可读 | 公开内容 |

---

## 4. 云函数设计

### 4.1 函数列表

```
cloudfunctions/
├── login                 # 登录，云函数触发
├── getUserInfo           # 获取用户信息
├── uploadImage           # 上传图片到云存储
├── analyzeImage          # AI图像分析
├── getReviewResult       # 获取评阅结果
├── saveReview            # 保存评阅记录
├── sendChat              # 发送对话消息
├── getCourses            # 获取课程列表
├── saveChatHistory       # 保存对话历史
└── getWorks              # 获取作品列表
```

### 4.2 核心函数流程

**图像分析流程**：
```
1. 小程序调用 uploadImage 上传图片
2. 获取云存储fileID
3. 调用 analyzeImage 云函数
4. 云函数下载图片，调用腾讯云AI
5. 返回分析结果（构图、光线、色彩评分）
6. 小程序展示评阅报告
```

**对话助手流程**：
```
1. 小程序调用 sendChat 发送消息
2. 云函数获取用户对话历史
3. 组装Prompt（注入摄影知识）
4. 调用LLM API获取回复
5. 保存对话历史
6. 返回回复给小程序
```

---

## 5. 目录结构

```
photography-review/
├── cloudfunctions/          # 云函数目录
│   ├── login/
│   ├── uploadImage/
│   ├── analyzeImage/
│   └── sendChat/
│
├── miniprogram/              # 小程序主体
│   ├── pages/
│   │   ├── index/           # 首页
│   │   ├── academy/          # 摄影学院
│   │   ├── review/           # 作品评阅
│   │   └── profile/          # 个人中心
│   │
│   ├── components/           # 组件
│   │   ├── floating-assistant/  # 悬浮助手
│   │   ├── work-card/       # 作品卡片
│   │   ├── course-card/     # 课程卡片
│   │   └── review-report/    # 评阅报告
│   │
│   ├── images/               # 图片资源
│   ├── styles/              # 全局样式
│   ├── utils/                # 工具函数
│   ├── app.js
│   ├── app.json
│   └── app.wxss
│
└── README.md
```

---

## 6. 开发规范

### 6.1 代码规范
- WXML标签闭合
- 样式采用BEM命名
- JS使用ES6+语法
- 云函数日志输出规范

### 6.2 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 页面 | 小写+横线 | `photo-review` |
| 组件 | 小写+横线 | `work-card` |
| 云函数 | 小写+横线 | `analyze-image` |
| 数据库集合 | 复数小写 | `users`, `works` |

### 6.3 安全规范
- 云函数环境变量存储密钥
- 图片URL不暴露真实存储路径
- 用户数据仅本人可访问
- AI调用增加频率限制

---

## 7. 第三方服务配置

### 7.1 腾讯云AI

```env
TENCENT_CLOUD_SECRET_ID=xxx
TENCENT_CLOUD_SECRET_KEY=xxx
TENCENT_CLOUD_APP_ID=xxx
```

### 7.2 LLM API

```env
LLM_API_KEY=xxx
LLM_API_ENDPOINT=xxx
```

### 7.3 配置方式
- 在云函数根目录创建 `cloudbaserc.json`
- 使用环境变量或配置中心存储密钥
- 不要将密钥提交到代码仓库

---

## 8. 部署流程

### 8.1 开发阶段
1. 本地编写云函数代码
2. 使用微信开发者工具测试
3. 触发云函数本地调试

### 8.2 上线阶段
1. 云函数上传并部署
2. 小程序上传代码
3. 在微信后台配置服务器域名
4. 提交审核发布

### 8.3 AI能力调试
- 腾讯云控制台查看调用日志
- LLM API后台查看token用量
- 云函数日志查看错误详情

---

## 9. 性能优化

### 9.1 首屏加载
- 分包加载，非首页代码懒加载
- 图片压缩后上传（最大20MB）
- 骨架屏占位，减少白屏

### 9.2 AI响应优化
- 图像分析结果缓存，避免重复调用
- 对话支持流式返回（打字机效果）
- 异步处理，不阻塞UI

### 9.3 图片加载
- 瀑布流图片懒加载
- 低质量占位图（blur）
- CDN加速分发

---

## 10. 注意事项

1. **免费额度**：腾讯云AI每月1000次，用完需要付费
2. **用户隐私**：用户上传图片和对话内容需要加密存储
3. **内容安全**：AI回复需要过滤，避免敏感内容
4. **接口限制**：LLM API有QPS限制，需要做请求排队
5. **版本管理**：AI模型更新需要记录版本号，便于追溯